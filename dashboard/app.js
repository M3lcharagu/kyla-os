const rooms=document.querySelectorAll('.room');const note=document.querySelector('#room-note');rooms.forEach(room=>{const focus=()=>{rooms.forEach(r=>r.classList.remove('selected'));room.classList.add('selected');note.textContent=`${room.querySelector('small').textContent} ${room.querySelector('h3').textContent} selected`;};room.addEventListener('click',focus);room.addEventListener('keydown',e=>{if(e.key==='Enter'||e.key===' '){e.preventDefault();focus();}})});const tip=document.querySelector('#tip');tip.addEventListener('click',()=>{tip.classList.toggle('done');tip.innerHTML=tip.classList.contains('done')?'Intention saved <span>✓</span>':'Mark intention <span>→</span>';});const clock=document.querySelector('#clock');function updateClock(){const d=new Date();clock.textContent=d.toLocaleTimeString([], {hour:'2-digit',minute:'2-digit',hour12:false});}updateClock();setInterval(updateClock,60000);